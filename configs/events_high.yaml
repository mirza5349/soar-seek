# Override file: merged on top of scenario_nominal.yaml by run scripts
ground_event_node:
  ros__parameters:
    event_lifetime_s: 75.0
    event_radius_m: 15.0
    event_rate_hz: 0.12
    high_priority_ratio: 0.6
    initial_event_count: 24
